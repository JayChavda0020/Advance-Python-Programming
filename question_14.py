# How many except statements can a try-except block have? Name Some built-in exception classes: 

'''ans : one except statement with each try block. ArithmaticError, AttributeError, Exception, ImportError, IndexError, NameError, OSError, 
RuntimeError, TabError, '''